// app-testid.patch — adds testID anchors to the L010 flow components (RNW emits data-testid).
// Apply from the app root:  git apply app-testid.patch   (or patch -p1 < app-testid.patch)
// Generated by maestro-dogfood-001. App source itself was reverted to keep the figma session clean.

--- a/components/ui/Button.tsx
+++ b/components/ui/Button.tsx
@@ -50,6 +50,8 @@
   onPress?: () => void;
   /** 스크린리더 라벨. icon 타입은 지정 권장(미지정 시 label 사용). */
   accessibilityLabel?: string;
+  /** 테스트 후킹 앵커 (RNW: testID → data-testid). */
+  testID?: string;
   /** 확장용 (레이아웃 등, 예: "w-full"). 뒤에 병합돼 오버라이드 가능. */
   className?: string;
 };
@@ -99,6 +101,7 @@
   disabled = false,
   onPress,
   accessibilityLabel,
+  testID,
   className,
 }: ButtonProps) {
   const s = SIZE[size];
@@ -121,6 +124,7 @@
 
   return (
     <Pressable
+      testID={testID}
       onPress={onPress}
       disabled={disabled}
       accessibilityRole="button"
--- a/components/ui/SocialLoginButton.tsx
+++ b/components/ui/SocialLoginButton.tsx
@@ -69,6 +69,7 @@
 
   return (
     <Pressable
+      testID={`signup-provider-${provider}`}
       onPress={() => onPress?.(provider)}
       accessibilityRole="button"
       accessibilityLabel={label}
--- a/components/ui/NavigationBar.tsx
+++ b/components/ui/NavigationBar.tsx
@@ -22,13 +22,15 @@
 type IconButtonProps = {
   source: ComponentProps<typeof Image>["source"];
   accessibilityLabel: string;
+  testID?: string;
   onPress?: () => void;
 };
 
 /** 48×48 아이콘 버튼 (Figma "Button" 인스턴스). DS Button 승격 시 교체 예정. */
-function IconButton({ source, accessibilityLabel, onPress }: IconButtonProps) {
+function IconButton({ source, accessibilityLabel, testID, onPress }: IconButtonProps) {
   return (
     <Pressable
+      testID={testID}
       onPress={onPress}
       accessibilityRole="button"
       accessibilityLabel={accessibilityLabel}
@@ -59,11 +61,13 @@
       <IconButton
         source={icChevronLeft}
         accessibilityLabel={backAccessibilityLabel}
+        testID="nav-back"
         onPress={onPressBack}
       />
       <IconButton
         source={icClose}
         accessibilityLabel={closeAccessibilityLabel}
+        testID="nav-close"
         onPress={onPressClose}
       />
     </View>
--- a/screens/SignupScreen.tsx
+++ b/screens/SignupScreen.tsx
@@ -56,7 +56,7 @@
         </View>
 
         {/* Title — gap 64 */}
-        <Text className="mt-16 text-center text-heading-lg text-label-normal">회원가입</Text>
+        <Text testID="signup-title" className="mt-16 text-center text-heading-lg text-label-normal">회원가입</Text>
 
         {/* Social stack — gap 48 · 버튼 사이 8 */}
         <View className="mt-12 gap-2">
@@ -79,6 +79,7 @@
         <View className="mt-12 flex-row items-center justify-center gap-1.5">
           <Text className="text-label text-label-neutral">이미 계정이 있으신가요?</Text>
           <Text
+            testID="signup-to-login"
             className="text-label text-primary-normal"
             accessibilityRole="link"
             onPress={() => onSelect("login")}
--- a/screens/ProviderResultScreen.tsx
+++ b/screens/ProviderResultScreen.tsx
@@ -26,8 +26,8 @@
       <NavigationBar onPressBack={onPressBack} onPressClose={onPressClose} />
 
       <View className="flex-1 items-center justify-center gap-3 px-8">
-        <Text className="text-label text-label-neutral">선택한 값</Text>
-        <Text className="text-title-lg text-label-normal">{value}</Text>
+        <Text testID="result-label" className="text-label text-label-neutral">선택한 값</Text>
+        <Text testID="result-value" className="text-title-lg text-label-normal">{value}</Text>
       </View>
     </View>
   );
--- a/screens/HomeScreen.tsx
+++ b/screens/HomeScreen.tsx
@@ -29,6 +29,7 @@
       <Button
         size="lg"
         label="로그인"
+        testID="home-login-cta"
         className="w-full"
         onPress={onPressLogin}
       />
