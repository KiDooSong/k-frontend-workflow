---
input_id: "IN-20260613-testid-001"
input_type: "testid"
source_type: "qa-automation"
source_ref: "qa/e2e-anchor-review/coupon-list-2026-06-13"
captured_at: "2026-06-13T00:00:00+09:00"
captured_by: "sample-testid-input-skill"
status: "captured"
confidence: "candidate"
affected_domains: ["coupons"]
affected_screens: ["COUPON-001"]
supersedes: null
---

# Input: 쿠폰 목록 E2E 안정 앵커 후보

## Summary
QA automation 리뷰에서 쿠폰 목록 화면의 안정 selector 후보를 제안했다. selector 는 구현 지시가 아니라 spec 이 선언하고 구현/tests 가 나중에 소비할 evidence 다.

## Extracted Facts
- 화면 타이틀 anchor 후보: `coupon-list-title`.
- 상태 탭 anchor 후보: `coupon-list-status-tabs` (D-001 이 separate-tab 으로 닫힐 때만 확정 가능).
- 쿠폰 카드 item anchor 후보: `coupon-list-item-{couponId}` (stableId 는 쿠폰 id).
- empty/error/retry 상태 anchor 후보가 필요하다.

## Suggested Target Artifacts
- COUPON-001 screen-spec (Accessibility — testID anchor draft/recommended note)
- testID intake note가 있는 repo라면 해당 note
- Verification item (D-001 이 열려 있어 tab 구조가 unresolved 이므로)

## Expected Reconciliation
- classification: simple-update + investigation-needed
- selector/testID 입력을 코드나 테스트 구현으로 반영하지 않는다.
- ScreenSpec 에 draft/recommended anchor note 를 남긴다.
- D-001 이 닫히기 전에는 상태 탭 testID 를 confirmed 로 올리지 않고, 필요하면 `VER-001` 로 검증 대기만 남긴다.

## Should Not Do
- Do not implement code directly from this input.
- Do not add or edit production tests directly from this input.
- Do not promote testID naming to confirmed without human approval.
- Do not edit generated files directly.
