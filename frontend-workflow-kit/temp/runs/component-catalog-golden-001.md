# Run report — component-catalog golden fixture + format freeze (001)

> Status: complete. Date: 2026-06-15.
> Branch: `test/mvp-c-catalog-golden` (new, off `origin/main` @ `3faf72d`, which carries the catalog-gen + #37 fixes).
> Worktree: `.claude/worktrees/catalog-golden` (main checkout untouched).
> Implements design §10 "FUTURE PR-4 — 골든 fixture".

---

## 0. Scope

Adds the first golden fixture for the `component-catalog` generator and **freezes its v1 output format**
by committing a byte-exact golden that the warning-first fixture harness compares against. Builds on the
generator already on main (skeleton + #37 anchor/wrapper fixes).

Changed files (allowed set only):
- `examples/component-catalog/basic-ui/**` — fixture (input tree + golden + run-metadata)
- `scripts/test-fixtures.mjs` — harness wiring (register `component-catalog` generated-view kind)
- this report

**NOT done (later PRs, per contract §6/§7/§10):** manifest `planned→active` flip, `do_not_edit` flip,
`check-generated-files` guard/allowlist registration, `package.json` `workflow:catalog` alias,
props/docgen/style. No generator change (`catalog-gen.mjs` untouched).

## 1. Fixture — `examples/component-catalog/basic-ui/`

```
basic-ui/
├─ run-metadata.json                 # { fixture:"component-catalog", src:"src", expected:"expected/...", expect:"pass" }
├─ src/components/ui/                 # INPUT — 3 components + 4 within-ui decoys
│   ├─ Button.tsx   export function Button            → INCLUDE
│   ├─ Card.tsx     export const Card = (p) => …      → INCLUDE (plain arrow const)
│   ├─ Stack.tsx    export function Stack             → INCLUDE
│   ├─ Badge.tsx    export const Badge = memo(…)      → EXCLUDE (memo wrapper)
│   ├─ Field.tsx    export const Field = forwardRef(…)→ EXCLUDE (forwardRef wrapper)
│   ├─ Modal.tsx    export default function Modal     → EXCLUDE (default export)
│   └─ index.ts     barrel re-exports                 → EXCLUDE (non-PascalCase basename)
├─ src/features/foo/components/ui/
│   └─ Ghost.tsx    export function Ghost             → EXCLUDE (nested non-canonical ui — locks the #37 anchor fix)
└─ expected/component-catalog.md      # GOLDEN (generated by the tool, not hand-authored)
```

Decoys cover every exclusion class in the design (§2): memo / forwardRef wrappers, default export,
non-PascalCase basename, and an out-of-canonical-scope nested `components/ui` (which the #37 anchor fix
now excludes — this fixture would have mis-included it before #37).

## 2. Frozen v1 output format (golden)

The golden locks the format the design left illustrative (OD-6 closed here):

```
# GENERATED FILE — DO NOT EDIT
<!-- Source: src/components/ui/** -->
<!-- Command: node scripts/catalog-gen.mjs --src src/components/ui --out docs/frontend-workflow/design/component-catalog.md -->

## Components

| Name | Source Path | Export Kind | Status |
| --- | --- | --- | --- |
| Button | src/components/ui/Button.tsx | named | ok |
| Card | src/components/ui/Card.tsx | named | ok |
| Stack | src/components/ui/Stack.tsx | named | ok |
```

H1 em-dash marker, `## Components` table (Name / Source Path / Export Kind / Status), sorted by
(source_path, name), no timestamp. This is the header form main already ships (the parallel skeleton),
so the earlier H1-vs-HTML-comment open question is settled as **H1** by this freeze.

## 3. Harness wiring (warning-first, no hard guard)

`test-fixtures.mjs` gains the `component-catalog` generated-view kind alongside route-tree/nav-graph —
the existing kind-agnostic `runGeneratedViewCase` runs the generator twice and checks determinism +
golden match. Four touch points: roots/script consts, the kind-switch (`inputKey`/`inputFlag`/`script`
→ `src`/`--src`/`catalog-gen.mjs`), `buildFixtures` registration, and the `runFixture` kind branch.
This rides the **warning-first `example:test`** path. The hard `check-generated-files` guard is **not**
touched (guard graduation is a later PR).

## 4. Verification (new worktree, absolute paths)

| Check | Result |
|---|---|
| `node --check scripts/test-fixtures.mjs` | OK |
| golden generated by tool; regenerate → `diff` | byte-identical (no diff) |
| catalog fixture in harness | verdict **pass** — `GV:run:1/2`, `GV:output:1/2`, `GV:deterministic`, `GV:content` all ok |
| `npm test` | 15 pass / 0 fail (test-fixtures incl. catalog + spec/api-manifest) |
| `npm run example:validate` | OK (검사 12종 통과) |
| NUL scan (golden + test-fixtures) | none |
| `git status` | only `M scripts/test-fixtures.mjs` + new `examples/component-catalog/**` |

## 5. Hard-rule compliance

No manifest flip / `do_not_edit` flip / guard registration / package alias / CI / props·style. The
generator (`catalog-gen.mjs`) is unchanged. The manual `component-catalog.md` is untouched.

## 6. Next

PR-5 — manifest `planned→active` + `do_not_edit:false→true` + `package.json` `workflow:catalog` alias.
PR-6 — guard graduation (`V1_ARTIFACT_IDS` + `V1_REPRODUCE` registration). Both deferred per design §10.
